For MAC users #Create a virtual environment - "python -m venv impresos"

For MAC users #Activate a virtual environment - "source impresos/bin/activate"

For install all the requirements - "pip install -r requirements.txt"